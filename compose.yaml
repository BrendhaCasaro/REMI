services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=remi-centraldb'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=myuser'
    ports:
      - '5432:5432'
